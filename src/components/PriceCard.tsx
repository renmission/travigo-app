import React from 'react'
import { Plans } from '../types'

interface IPlansProps { 
    plans: Plans[];
}

const PriceCard = ({ plans }:IPlansProps ) => {
    console.log(plans);
    
  return (
    <>
      
    </>
  )
}

export default PriceCard
